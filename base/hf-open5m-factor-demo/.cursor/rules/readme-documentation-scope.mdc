---
description: 仅约束仓库根目录 README.md 的编写与改写（读者、允许/禁止及自检）
globs: README.md
alwaysApply: false
---

# README.md 文档边界（面向 Agent）

**生效方式**：`globs` + `alwaysApply: false` 对应官方 **「Apply to Specific Files」**——在上下文中出现匹配文件时自动附加；不必每个会话常驻。若需未打开文件也加载全文，可改为 **`alwaysApply: true`**（会占用更多通用上下文）。`globs: README.md` 按官方 glob 表为**仓库根目录**该文件名（若你使用的 Cursor 版本把子目录同名文件也算作匹配，再收窄模式并以发行说明为准）。

## 读者与用途

根目录 **`README.md`** 主要面向：

- **读者**：了解本项目做什么、三种接入形态与三种策略模式在**配置与运行现象**上的差异；
- **轻量开发者**：会改 JSON、会构建运行、会在 `factors/`、`models/` 里按示例扩展因子与模型。

**不是**面向：深度改构建系统、CMake、分发流水线、引擎内部默认矩阵的**框架维护者**专读文档；后者仅在**维护用工作区**（含 **`AGENTS.md`** 与 **`.cursor/rules/*.mdc`**）中维护——**分发到各子项目时不会携带** `AGENTS.md` 与 **`.cursor/`**，故 **README 不得依赖或链接这些路径**。

## 允许写入 README 的内容

- 项目定位、术语（接入形态 / 策略模式）、与现场配置对齐的**使用说明**；
- **`config_*.json` 字段**在文档层面的归纳（以样例与解析代码为准的提醒可写，但不要逐文件复述实现）；
- **`make help` / `make build-*` / 运行入口**（脚本参数以 `--help` 为准类表述）；
- **如何新增因子与模型**的路径级指引（目录、注册方式、注意事项），与数据流**示意**级说明；
- 指向 **`docs/ev_management.md`** 等**子项目仓库内确实存在**的文档；**不要**在 README 中加入 **`AGENTS.md`**、**`.cursor/`** 或「见某仅维护区才有的文件」类链接（分发子项目不含上述路径）。

## 禁止或应避免写入 README 的内容

- **编译期实现细节**：例如 CMake 变量名、生成的 **`ENABLE_*` 宏名**、具体头文件路径（如 `runtime_policy.h`）、C++ 类型名（如 `runtime_policy::StrategyMatrix`）等；若与策略模式相关，写入**维护区**的 **`AGENTS.md`** 并/或在 **`agent-project-guide.mdc`** 展开，**不要**写进 README。
- **Makefile / CMake 推断规则、CGP 拷贝字段** 等维护者信息：只放在 **`agent-project-guide.mdc`** 或 **`AGENTS.md`**，README 至多写「详见 `make help`」类**无源码耦合**的提示。
- **内网地址、密钥、token**（与维护区 `AGENTS.md`「不要写入」一致）。
- 在 README 中**展开** `.cursor/` 规则全文，或**引导读者打开** `AGENTS.md` / `.cursor/`（子项目读者无法访问）。

## 仓库形态：不得让读者推断「另有统一根项目 / 分发关系」

`README.md` 的读者可能只持有**单一接入形态**的独立工程；文档应始终**以当前仓库根目录下实际存在的目录与配置为准**，不得写出或拼凑出下列**会令读者怀疑还存在「总仓、母工程、完整聚合树」或「从某处拷贝/分发得到本树」**的线索：

- **禁止**：`CGP`、`cgp/config`、拷贝任务、`source_base` / `target_base`、分发/同步流水线、**「一体化源码目录」相对「分发仓库」的对照**、九项目/九份 README 的改名映射、**多份归档 README 的具体文件名模板**（如 `README_open_*.md` 与目标 `README.md` 的对应规则）等。
- **允许**：说明「根目录常见只含一类 `app_*`」「与当前目录不一致的章节可跳过」「对照表用于理解命名，以本仓 `config_*.json` 为准」等——仅描述**本仓读者可见事实**，不指名工程外部的组织方式。
- **用词**：避免在 README 中使用易与「多 `app_*` 聚合一棵树」混淆的 **「一体化仓库」「母仓」「源码总树」** 等；若需表达 Live 侧「因子与模型同链路」，优先用 **「端到端」「同进程/同动态库链路」** 等不含源码拓扑暗示的说法。

## 修改 README 时的自检

在提交对 **`README.md`** 的改动前，确认新增段落**仍能被「只读 README 的使用者/轻量开发者」直接消费**；若段落主要为 Agent 或维护者排查服务，应迁移到**维护区**的 **`AGENTS.md`** 或 **`agent-project-guide.mdc`**，且 **README 内不得留下对这两处路径的依赖**（分发子项目无此文件）。

额外过一遍**仓库形态**小节：全文读下来，读者是否仍只会把「本项目」理解为**当前打开的这一个仓库**；若出现任何可映射到「另有完整根项目 / 分发来源」的专有名词或文件命名模板，应删改或下放到 **`AGENTS.md`** / **`agent-project-guide.mdc`**。
